# Spring Boot Payments Sample

Prepared sample for the Dev Spaces demo. Already carries a `devfile.yaml`, so it
starts as a fully configured workspace from a single Git URL.

Used in acts 2 through 4 of the demo. See `../../docs/RUNBOOK.md`.
